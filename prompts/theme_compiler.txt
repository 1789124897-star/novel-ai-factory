【主题解析与故事内核生成器 · 必须首先执行】
接收到动态主题 `{theme}` 后，AI需在内部进行以下三步解析，生成不可变的叙事锚点：

第一步：象征与属性挖掘
- **物理属性**：列出 `{theme}` 核心的3-5种可感知特征（如颜色、质地、声音、气味、变化过程）。
- **文化/隐喻象征**：列出2-3种常见的正面与负面象征意义（如"玫瑰"象征爱情与危险；"钟表"象征时间与禁锢）。
- **可操作性**：列出2-3种与该主题相关的、可用于"犯罪/仪式/隐匿"的具体行为（如"墨水"可用于伪造、下毒、书写诅咒；"藤蔓"可用于缠绕、窒息、传递信息）。

第二步：核心事件推导（根据上述属性生成）
- **事件类型**：从以下选项中选择最契合的一项生成具体事件：
  A. **偏执的保存**：（角色甲）为永久留住（角色乙/某状态），滥用 `{theme}`，导致（悲剧后果）。
  B. **污染的馈赠**：一份与 `{theme}` 相关的"礼物/遗产"被证明是缓慢生效的毒药或诅咒。
  C. **隐秘的置换**：利用 `{theme}` 的某种特性（如可复制、可燃烧、可生长）来掩盖一桩罪行或一个秘密。
  D. **仪式的失败**：一场围绕 `{theme}` 进行的、旨在达成某种目的（如复活、诅咒、通灵）的古老仪式失控，留下不可磨灭的创伤。
- **事件要素**：明确该事件中的**死者/受害者**、**主动发起者**、**物理空间**，以及事件留下的**核心物证**。

第三步：人物与闭环预设
- **女主继承/进入的原因**：必须直接源于"核心事件"（如继承遗产、受托调查、被迫避难）。
- **双男主动机锚定**：
  - 男1的动机必须与 **"美化/修复/合理化"** 该核心事件相关。
  - 男2的动机必须与 **"揭露/破坏/利用"** 该核心事件相关。
  - 两者的方式都必须偏执且与 `{theme}` 的属性强绑定。
- **闭环预言生成**：基于核心事件的悲剧本质，生成一句模糊、诗意、可多重解读的"预言"。该预言必须包含 `{theme}` 的意象，并在结尾实现语义的恐怖升级。
